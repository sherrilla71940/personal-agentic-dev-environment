{{- /* The shared working agreement is inlined from .chezmoitemplates/core.md.
       Edit the body there, not here. Codex reads the same body as AGENTS.md and
       Copilot as core.instructions.md.
       Everything below the marker is Claude Code-specific and is deliberately
       absent from the shared body -- it is not reworded into a neutral twin. */ -}}
<!-- Rendered by chezmoi from the developer environment repo. Editing THIS file does nothing durable:
     it is a template, so `chezmoi re-add` skips it and the next `chezmoi apply` reverts it.

       Rules above the "Claude Code only" marker are shared with Codex and Copilot.
         edit  home/.chezmoitemplates/core.md        or run  dotf-core
         edit  home/.chezmoitemplates/continuity.md

       Rules below the marker are Claude Code-specific.
         edit  home/dot_claude/CLAUDE.md.tmpl      or run  dotf-claude

     Claude Code strips block-level HTML comments before loading, so this note costs no
     context. Codex and Copilot do not strip them, which is why it lives here and not in
     the shared body. -->
{{ includeTemplate "core.md" . -}}
{{- includeTemplate "continuity.md" . }}
# Claude Code only

Everything below depends on a Claude Code feature that Codex and Copilot do not have.

## Response behavior

- **Verify version-sensitive Claude details before acting.** Before changing Claude Code, Claude Agent SDK, or Claude API configuration—or giving exact flags, setting keys, hook events, defaults, model IDs, or command syntax—check the built-in `claude-code-guide` agent or current official documentation. Memory and skills may guide where to look, but are not authoritative when behavior can drift.

## Client surfaces

- When answering about a Claude Code picker, slash command, keyboard shortcut, panel, or other UI
  affordance, name the client surface first: terminal TUI, VS Code extension, or desktop app.
  State when the behavior differs between surfaces or is unverified on the user's surface. In
  particular, terminal `/resume` picker shortcuts do not describe the VS Code extension's session
  panel picker; if a user says guidance failed, establish the surface and cause before retracting
  guidance that may be correct elsewhere.
- After editing user-level Claude configuration, identify the authoritative source and the rendered
  target separately, say whether `chezmoi apply` has actually run, and state the reload boundary.
  `settings.json` (including hooks), `skills/*/SKILL.md`, `agents/*.md`, and `keybindings.json`
  are watched or hot-reloaded by supported Claude versions; `CLAUDE.md`, `commands/*.md`, and a
  hand-edited `.mcp.json` require a new session. Do not tell the user to restart when the edit was
  only made in chezmoi source and has not yet been applied; the change has not reached Claude's
  target directory.

## Asking the user

- Use an `AskUserQuestion` picker when the decision is **where work lands or whether something is
  kept**, and the options are a small closed set you could otherwise settle unilaterally and get
  wrong: which branch or worktree a fix belongs on, whether repository infrastructure rides along
  in the current request or takes its own branch, whether a file is filed and where. Prose there
  makes the user retype an answer a click could give.
- A picker needs options that already exist. Ask an open-ended question — which account, what
  threshold, what wording — plainly instead; settle anything with a conventional default yourself;
  and skip the question entirely for a disposable attachment or a file already sitting somewhere
  stable.

## Session workflow

- When closing out an active item in auto memory, drop it from the tracked list; only keep its outcome in a topic file if it could still affect a future decision.

## Parallelizing independent work

- When a task decomposes into substantial independent units, consider parallel `Agent` calls when all of these are true: ownership boundaries are explicit, outputs can be verified independently, agents will not modify shared files or generated artifacts, and the expected speed benefit exceeds briefing and integration cost.
  <!-- Personal Notes: -->
  <!-- - Subagents do NOT inherit the parent session's auto memory (confirmed via Claude Code docs — the main exception is a fork, which inherits the parent conversation). Any project fact, decision, or history a subagent needs must be written into its prompt explicitly; don't assume it can look this up itself. -->
  <!-- - Worktree isolation for parallel subagents is opt-in, not automatic — request it explicitly (`isolation: 'worktree'` on the Agent call, or ask Claude to "use worktrees for your agents") whenever the parallel agents will write to overlapping files. Nothing creates a worktree silently. -->
  <!-- - Keep the managed `git wt-add` and `git wt-copy` workflow. It is useful here because Claude, Codex, VS Code, and terminal Git can otherwise create worktrees without the same ignored local instructions and development configuration. The wrapper preserves native Git branch, path, and start-point arguments, refuses overwrites, and can provision an existing worktree. Example: `git wt-add -- -b feat/example ../example develop`. See `docs/worktree-provisioning.md` in the developer environment repository for the full behavior and safety boundaries. -->
- Subagents do not inherit the parent conversation or the project's auto memory (a fork is the exception). Ordinary custom subagents **do** load the CLAUDE.md hierarchy; the built-in `Explore` and `Plan` agents skip it. Brief each subagent with any required parent-conversation or auto-memory context, and restate CLAUDE.md rules only for agents that do not load them — proportionate to the task, not a blanket context dump.
- Reassess mid-task if the remaining work becomes independently parallelizable. Keep work sequential when steps share architectural or git state, depend on earlier results, overlap files, or are too small to justify delegation.
- Agent Teams require the user's approval. Do not treat this preference as standing authorization to create a team.

## Shell tool preference

Both shell tools can be registered at once. On Windows the PowerShell tool is on by default; on macOS and Linux it is opt-in and needs PowerShell 7+ (`pwsh`) on `PATH`, so the choice usually only arises on Windows.

- Prefer the **Bash tool** for standard operations (`mv`, `mkdir`, `ls`, `grep`, `git`, etc.) — Git Bash backs it and these are simpler and more portable than PowerShell equivalents. (`CLAUDE_CODE_USE_POWERSHELL_TOOL=0` in settings.json forces the Bash tool on Windows even when the PowerShell-tool rollout is active.)
  <!-- Personal Notes: -->
  <!-- source (CLAUDE_CODE_USE_POWERSHELL_TOOL=0): code.claude.com/docs/en/setup → Windows setup — "Set CLAUDE_CODE_USE_POWERSHELL_TOOL=1 to opt in or 0 to opt out." Also verified empirically this session: the PowerShell tool became unavailable once =0 took effect. -->
- Use the **PowerShell tool** only when the task is genuinely Windows-specific: COM automation, registry access, or PowerShell-only cmdlets.
- If the Bash tool is unavailable, say so before falling back to PowerShell.
